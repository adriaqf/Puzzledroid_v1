<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.Toolbar xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:layout_alignParentBottom="true"
    android:layout_gravity="bottom"
    app:contentInsetLeft="0dp"
    app:contentInsetStart="0dp"
    app:contentInsetRight="0dp"
    app:contentInsetEnd="0dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal">

                <Button
                    android:id="@+id/btn_back_home"
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:layout_weight="1"
                    android:background="@drawable/button_toolbar_bottom"
                    android:gravity="center"
                    android:onClick="onClickGoTo"
                    android:text="Atrás"
                    android:textColor="@color/background_text_bottom_toolbar"
                    android:textStyle="bold" />

                <View
                    android:layout_width="1dp"
                    android:layout_height="match_parent"
                    android:background="@color/toolBarBtnGrad2" />


                <Button
                    android:id="@+id/btn_classification"
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:layout_weight="1"
                    android:background="@drawable/button_toolbar_bottom"
                    android:gravity="center"
                    android:onClick="onClickGoTo"
                    android:text="Clasificación"
                    android:textColor="@color/background_text_bottom_toolbar"
                    android:textStyle="bold" />

        </LinearLayout>

</androidx.appcompat.widget.Toolbar>